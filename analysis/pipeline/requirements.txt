# 안심존 현장은 인터넷이 막혀 있을 수 있습니다. 방문 전에
#   pip download -r requirements.txt -d vendor/
# 로 wheel 파일을 미리 받아 USB로 반입하고,
#   pip install --no-index --find-links=vendor -r requirements.txt
# 로 오프라인 설치하세요. (반입 자체가 안심존 보안 정책상 가능한지 사전 확인 필요)

duckdb>=1.0
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
shap>=0.45
